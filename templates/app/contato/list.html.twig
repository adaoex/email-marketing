{% extends '@layout/default.html.twig' %}

{% block title %}
    Lista de Clientes - {{ parent() }}
{% endblock %}

{% block page_header %}
    Listagem de Clientes
{% endblock %}

{% block content %}
    <div class="row" >
        <div class="box" >
            <div class="box-header" >
                <a class="btn btn-app" href="{{ url('contato.create') }}" >
                    <i class="fa fa-plus" ></i>
                    Novo
                </a>
            </div>
            <div class="box-body" >
                <table class="table table-bordered" >
                    <thead>
                        <tr>
                            <th style="width: 10px;" >ID</th>
                            <th>Nome</th>
                            <th>E-mail</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for contato in contatos %}
                        <tr>
                            <td>{{ contato.id }}</td>
                            <td>{{ contato.nome }}</td>
                            <td>{{ contato.email }}</td>
                            <td>
                                <a href="{{ url('contato.update', {id: contato.id}) }}" 
                                   class="btn btn-default">
                                    <i class="fa fa-edit" ></i>
                                    Editar
                                </a>
                                <a href="{{ url('contato.delete', {id: contato.id}) }}" 
                                   class="btn btn-danger">
                                    <i class="fa fa-remove" ></i>
                                    Excluir
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}