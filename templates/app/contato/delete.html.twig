{% extends '@layout/default.html.twig' %}

{% block title %}
    Exclusão de Contato - {{ parent() }}
{% endblock %}

{% block page_header %}
    Exclusão do Contato - {{ contato.nome }}
{% endblock %}

{% block content %}
    <div class="row" >
        <div class="col-md-8">
            <div class="box box-danger" >
                <div class="box-body" >
                    <form name="form_disable" >
                        <fieldset disabled >
                            <div class="box-body" >
                                <div class="form-group">
                                    <label for="nome">Nome: </label>
                                    <input type="text" name="nome" value="{{ contato.nome }}" class="form-control" />
                                </div>
                                <div class="form-group">
                                    <label for="email">E-mail: </label>
                                    <input type="email" name="email" value="{{ contato.email }}" class="form-control" />
                                </div>
                            </div>
                        </fieldset>
                    </form>
                    <form name="form" role="form" method="post" action="{{ url('contato.delete', {id: contato.id}) }}" >
                        <input type="hidden" name="_method" value="DELETE" />
                        <div class="box-footer" >
                            <button class="btn btn-danger" type="submit" >
                                <i class="glyphicon glyphicon-remove" ></i>
                                Excluir
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}